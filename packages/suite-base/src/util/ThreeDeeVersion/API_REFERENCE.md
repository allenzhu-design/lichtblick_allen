# ThreeDeeVersion API 快速参考\n\n## 核心 API\n\n### 版本管理 (`version.ts`)\n\n```typescript\n// 获取当前版本\ngetThreeDeeVersion(): 'standard' | 'custom'\n\n// 设置版本（自动保存到 localStorage）\nsetThreeDeeVersion(version: 'standard' | 'custom'): void\n\n// 版本检查\nisCustomVersion(): boolean\nisStandardVersion(): boolean\n\n// 从 localStorage 恢复版本\nloadVersionPreference(): void\n\n// 版本转 panel 类型\nversionToPanelType(version): '3D' | '3D_custom' | ...\n\n// Panel 类型转版本\npanelTypeToVersion(panelType): 'standard' | 'custom'\n```\n\n### 配置管理 (`configManager.ts`)\n\n```typescript\n// 同步获取默认配置（无需 await）\ngetDefaultCameraState()\ngetDefaultPublishSettings()\ngetDefaultSceneExtensionConfig()\ngetDefaultRendererConfig()\n```\n\n### 工具函数 (`utils.ts`)\n\n```typescript\n// 识别 panel 类型\nisCustomPanelType(panelType: string): boolean\n\n// 获取基础类型（去后缀）\ngetBasePanelType(panelType: string): string\n\n// 获取版本标签\npanelTypeToVersionLabel(panelType): 'custom' | 'standard'\n\n// 获取 UI 显示名称\ngetPanelDisplayName(panelType): string\n// '3D' → '3D'\n// '3D_custom' → '3D (Custom)'\n// 'Image' → 'Image'\n// 'Image_custom' → 'Image (Custom)'\n```\n\n## 导入示例\n\n```typescript\n// 导入单个函数\nimport { getThreeDeeVersion } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\n// 导入多个函数\nimport {\n  getThreeDeeVersion,\n  setThreeDeeVersion,\n  getDefaultRendererConfig,\n  getPanelDisplayName,\n} from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\n// 导入类型\nimport type { ThreeDeeVersion, ThreeDeePanel } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n```\n\n## 常见用例\n\n### 1. 应用启动时恢复用户偏好\n```typescript\nimport { loadVersionPreference } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\n// 在应用初始化时调用\nloadVersionPreference();\n```\n\n### 2. 获取 panel 显示名称\n```typescript\nimport { getPanelDisplayName } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\nconst panelType = '3D_custom';\nconst name = getPanelDisplayName(panelType); // '3D (Custom)'\n```\n\n### 3. 检查 panel 是否为自定义版本\n```typescript\nimport { isCustomPanelType } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\nif (isCustomPanelType(panelType)) {\n  // 处理自定义版本特定逻辑\n}\n```\n\n### 4. 获取默认配置初始化 panel\n```typescript\nimport { getDefaultRendererConfig } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\nconst config = getDefaultRendererConfig();\n// {\n//   cameraState: DEFAULT_CAMERA_STATE,\n//   publish: DEFAULT_PUBLISH_SETTINGS,\n//   ...\n// }\n```\n\n### 5. 用户更改版本时保存\n```typescript\nimport { setThreeDeeVersion } from '@lichtblick/suite-base/util/ThreeDeeVersion';\n\n// 用户在 UI 中选择 \"3D Custom\"\nsetThreeDeeVersion('custom');\n// 自动保存到 localStorage\n```\n\n## 文件统计\n\n| 文件 | 行数 | 状态 |\n|-----|------|------|\n| version.ts | ~80 | ✓ 优化 |\n| configManager.ts | ~40 | ✓ 优化 |\n| utils.ts | ~30 | ✓ 优化 |\n| index.ts | ~8 | ✓ 优化 |\n| typeAdapter.ts | ~2 | ✗ 弃用 |\n| **总计** | **~160** | 🎉 减少 70+ 行 |\n\n## localStorage 键值\n\n- **键**：`lichtblick_threedee_version`\n- **值**：`'standard'` 或 `'custom'`\n- **初始值**：`'standard'`（如果不存在则使用默认值）\n\n## 变更日志\n\n### 优化内容\n- ✅ 移除复杂的动态导入逻辑\n- ✅ 简化配置管理（同步操作）\n- ✅ 弃用类型适配器（通过导入重新导出解决）\n- ✅ 添加 localStorage 支持\n- ✅ 清理导出，移除不必要的默认导出\n\n### 向后兼容性\n- ✅ 所有公共 API 保持不变\n- ⚠️ 移除了 `importThreeDeeModule()` 函数（已不需要）\n- ⚠️ 移除了 `getModulePath()` 函数（已不需要）\n- ⚠️ 移除了 typeAdapter 中的类型转换函数\n\n如果你的代码使用了这些已移除的函数，改用直接的 import 语句。\n
